"use client";
import {useState} from "react";

const prompts=["Why is Secondary collection below target?","Show unmatched payments needing review","Summarize this month's expenses","Which accounts have the largest balances?","What changed in collections this week?"];
export default function FinanceAIPage(){const [q,setQ]=useState("");const [asked,setAsked]=useState(prompts[0]);return <main><header className="fo-head"><div><span>FINANCE OFFICE · AI</span><h1>Finance AI</h1><p>Ask operational finance questions using authorized billing, transaction, reconciliation and reporting context.</p></div></header><section className="fo-ai"><h2>Finance Intelligence Assistant</h2><p>Designed to explain collection movement, reconciliation exceptions, balances, payment plans, expenses and reports. It should show evidence and never make hidden credit decisions about families.</p></section><section className="fo-grid two"><article className="fo-card"><header><div><h2>Ask Finance</h2><p>Prototype conversation using mock data.</p></div></header><div className="fo-list"><div><strong>YOU</strong><span>{asked}</span></div><div><strong>SCHOOLOS FINANCE AI</strong><span>Secondary collection is currently 86%, below the 92% owner target. The visible drivers in this mock data are open balances and scheduled payment plans. I cannot infer why individual families have not paid, and I should not rank or judge families from payment behavior.</span></div></div><div className="fo-prompt" style={{marginTop:12}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask a finance question..."/><button onClick={()=>{if(q.trim()){setAsked(q.trim());setQ("")}}}>Ask AI</button></div></article><aside className="fo-card"><header><div><h2>Suggested questions</h2></div></header><div className="fo-list">{prompts.map(p=><div key={p} onClick={()=>setAsked(p)} style={{cursor:"pointer"}}><strong>{p}</strong><span>Uses finance-authorized aggregate context.</span></div>)}</div></aside></section><section className="fo-grid equal"><article className="fo-card"><div className="fo-callout">Finance AI may summarize and flag anomalies, but refunds, reversals, financing approvals, payroll payments and write-offs require authorized human action.</div></article><article className="fo-card"><div className="fo-callout">Payment history must remain factual. Do not turn it into a hidden family credit score or use it to influence a child's academic treatment.</div></article></section></main>}
